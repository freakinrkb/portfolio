var options = {
    strings: ["coder", "designer", "developer","student"],
    loop : true,
    typeSpeed: 40,
    backSpeed : 60,
    backDelay : 1500
  };
  
  var typed = new Typed('.multitext', options);